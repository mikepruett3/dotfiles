---

- name: "Install packages (pacman)"
  community.general.pacman:
    name:
      - bat
      - bash-completion
      - btop
      - curl
      - dos2unix
      - eza
      - fontconfig
      - fzf
      - git
      - glow
      - go
      - gnupg
      - htop
      - jq
      - mediainfo
      - neovim
      - pass
      - poppler
      - pydf
      - p7zip
      - unrar-free
      - unzip
    state: present

- name: "Install packages from AUR (paru)"
  ansible.builtin.shell:
    cmd: paru -S --needed --noconfirm --noprogressbar {{ item }}
  with_items:
    - fuse2
  become: yes
  become_user: "{{ ansible_facts['user_id'] }}"
  when:
    - desktop is defined
    - aur_helper == 'paru'

- name: "Install Desktop packages (pacman - hyprland)"
  community.general.pacman:
    name:
      - avahi
      - chromium
      - cups
      - firefox
      - fuse
      - gamemode
      - hypridle
      - hyprlock
      - kanshi
      - kitty
      - lib32-gamemode
      - libmupdf
      - mako
      - mpv
      - mupdf-gl
      - nss-mdns
      - pamixer
      - pciutils
      - swww
      - usbutils
      - viewnior
      - wofi
      - waybar
      - xdg-desktop-portal-hyprland
      - yt-dlp
    state: present
  when:
    - desktop is defined
    - desktop == 'hyprland'

- name: "Install Yay Package Manager"
  ansible.builtin.include_role:
    name: jahrik.yay
  vars:
    install: true
  when:
    - desktop is defined
    - desktop == 'hyprland'

- name: "Install Desktop packages (yay)"
  ansible.builtin.shell:
    cmd: yay -S --needed --noconfirm --noprogressbar {{ item }}
  with_items:
    - cdw
    - git-credential-manager
    #- ludusavi
    - hyprshot
    - pwvucontrol
    - sddm-theme-tokyo-night-git
    - streamdeck-ui
    #- ttf-hack-nerd
    - wpaperd
  become: yes
  become_user: "{{ ansible_facts['user_id'] }}"
  when:
    - desktop is defined
    - desktop == 'hyprland'
    - aur_helper == 'yay'
